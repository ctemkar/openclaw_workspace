### Dashboard Access Issue
*   User is seeing the message "Flask server is running. Access the dashboard via /static/index.html or similar." when accessing http://127.0.0.1:62347/
*   This is the message from the `home` route in `app.py` that I had set as a placeholder.
*   The issue is that the Flask app's root route (`/`) is returning this basic message instead of serving the actual dashboard HTML.
*   The dashboard should be accessible at `/static/index.html` as suggested, or the root route needs to be fixed to serve the dashboard properly.